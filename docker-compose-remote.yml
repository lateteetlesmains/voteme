version: "2"
services:
  web:
    image: adl1422/voteme:latest
    container_name: web_app
    restart: always
    ports:
      - '8080:8080'
    depends_on:
      - redis
    privileged: true

  redis:
    image: redis:5
    container_name: redis
    restart: always
    ports:
      - '6379:6379'
